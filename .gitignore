# Rust
/target
**/*.rs.bk

# Node / TypeScript
node_modules
dist
*.tsbuildinfo

# NAPI build artifacts (the .node binaries; index.js/index.d.ts ARE committed —
# they're the platform-independent loader + types the published package needs)
*.node

# undo's own working dir (never commit captured snapshots)
.undo

# OS
.DS_Store

# napi release staging (regenerated in CI)
crates/undo-napi/npm/
crates/undo-napi/artifacts/
